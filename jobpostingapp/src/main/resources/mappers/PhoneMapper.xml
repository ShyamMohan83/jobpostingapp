<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobposting.jobpostingapp.mapper.PhoneMapper">
    <insert id="insertPhoneNumbers" keyProperty="phoneId" keyColumn="phone_id"  useGeneratedKeys="true"
      parameterType="com.jobposting.jobpostingapp.model.Phone">
    INSERT INTO phones(
            user_id,
            phone_number,
            type,
            is_primary,
            is_verified
        ) VALUES(
            #{userId},
            #{phoneNumber},
            #{type},
            #{isPrimary},
            #{isVerified}
        )
    </insert>

</mapper>