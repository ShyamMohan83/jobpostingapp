<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jobposting.jobpostingapp.mapper.AuthMapper">
    <resultMap id="UserAuthEntity" type="com.jobposting.jobpostingapp.model.UserAuthEntity">
        <id property="userId" column="user_id" />
        <result column="username" property="userName" />
    </resultMap>
    <select id="getPasswordByUsername" resultMap="UserAuthEntity">
        SELECT password_hash, username, user_id
        From user_auth WHERE username= #{userName}
    </select>
    
    <insert id="insertAuthInformation" keyColumn="auth_id" keyProperty="authId"
            useGeneratedKeys="true"  parameterType="com.jobposting.jobpostingapp.model.UserAuthEntity">
        INSERT INTO user_auth(
        user_id,
        username,
        password_hash,
        auth_provider
        )
        VALUES(
        #{userId},
        #{userName},
        #{passwordHash},
        #{authProvider}
        )

    </insert>
</mapper>